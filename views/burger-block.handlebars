<li>
	{{burger_name}}

	<button class="change-burgers" data-id="{{id}}" data-newburger="{{burger}}">
		{{#if burger}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burgers" data-id="{{this.id}}">DELETE!</button>
</li>
